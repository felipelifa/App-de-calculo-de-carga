[build]
  base = "."
  command = "cd app && git clone https://github.com/flutter/flutter.git --depth 1 -b stable flutter_sdk && export PATH=\"$PATH:$(pwd)/flutter_sdk/bin\" && flutter config --enable-web && flutter build web --release --web-renderer html"
  publish = "app/build/web"
