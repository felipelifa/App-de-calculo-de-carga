[build]
  base = "app"
  command = "if [ ! -d 'flutter' ]; then git clone https://github.com/flutter/flutter.git -b stable; fi && ./flutter/bin/flutter pub get && ./flutter/bin/flutter build web --release"
  publish = "build/web"
