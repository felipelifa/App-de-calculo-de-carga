[build]
  base = "app"
  command = "git clone https://github.com/flutter/flutter.git -b stable --depth 1 flutter_sdk && ./flutter_sdk/bin/flutter build web --release --web-renderer html"
  publish = "build/web"
