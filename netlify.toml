[build]
  base = "."
  command = "cd app && git clone https://github.com/flutter/flutter.git --depth 1 -b stable flutter_sdk && ./flutter_sdk/bin/flutter pub get && ./flutter_sdk/bin/flutter build web --release --web-renderer=html"
  publish = "app/build/web"
