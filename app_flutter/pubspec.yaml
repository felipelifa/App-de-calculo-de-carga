name: app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+2

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.1.5+1
  google_fonts: ^8.0.2
  go_router: ^17.1.0
  syncfusion_flutter_charts: ^33.1.45
  flutter_animate: ^4.5.2
  cached_network_image: ^3.4.1
  intl: ^0.19.0
  timezone: ^0.10.1
  flutter_local_notifications: ^18.0.1
  url_launcher: ^6.3.2
  http: ^1.2.0
  shared_preferences: ^2.5.2
  
  # Vision Nutrition Engine
  mobile_scanner: ^5.0.0        # Barcode scanner com câmera
  image_picker: ^1.0.0          # Captura de foto
  google_generative_ai: ^0.4.0  # Gemini Vision para reconhecimento
  supabase_flutter: ^2.17.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/data/tbca_minified.json
